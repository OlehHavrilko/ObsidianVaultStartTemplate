# 📋 Дашборд задач

## 🎯 Активные проекты (1. Проекты/Активные)
```dataview
TABLE status as "Статус", updated as "Обновлено"
FROM "1. Проекты/Активные"
WHERE status = "активный"
SORT updated DESC
```

## 📅 Задачи на сегодня
```dataview
TASK
WHERE !completed AND due <= date(today)
SORT due ASC
```

## 📅 Предстоящие задачи
```dataview
TASK
WHERE !completed AND due > date(today)
SORT due ASC
LIMIT 15
```

## 📝 Задачи без срока
```dataview
TASK
WHERE !completed AND (due = null OR due > date(today))
SORT file.ctime DESC
LIMIT 10
```

## 📊 Статистика задач
```dataview
TABLE WITHOUT ID
  length(filter(allTasks, (t) => !t.completed)) as "Активных",
  length(filter(allTasks, (t) => t.completed)) as "Завершено",
  round(length(filter(allTasks, (t) => t.completed)) / length(allTasks) * 100, 1) as "Прогресс %"
FROM #task
```

---
[[Дашборд PARA|← На главный дашборд]]