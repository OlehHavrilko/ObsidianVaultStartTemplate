# 🏠 Дашборд PARA

Система организации на основе метода Tiago Forte

---

## 📅 СЕГОДНЯ
`= dateformat(date(today), "cccc, dd MMMM yyyy")`

## 🎯 АКТИВНЫЕ ПРОЕКТЫ (1. Проекты/Активные)
```dataview
TABLE status as "Статус", updated as "Обновлено"
FROM "1. Проекты/Активные"
WHERE status = "активный"
SORT updated DESC
```

## 📝 ЗАДАЧИ НА СЕГОДНЯ
```dataview
TASK
WHERE !completed AND due <= date(today)
SORT due ASC
LIMIT 10
```

## 📥 INBOX (5. Входящие)
```dataview
TABLE file.ctime as "Создано"
FROM "5. Входящие"
SORT file.ctime DESC
LIMIT 5
```

## 📚 РЕСУРСЫ ДЛЯ ИЗУЧЕНИЯ (3. Ресурсы)
```dataview
TABLE tags as "Темы", created as "Добавлено"
FROM "3. Ресурсы"
SORT created DESC
LIMIT 5
```

## 🏡 ОБЛАСТИ ЖИЗНИ (2. Области)
```dataview
TABLE length(file.inlinks) as "Связей"
FROM "2. Области"
SORT length(file.inlinks) DESC
```

## 📊 ПРОГРЕСС ПРОЕКТОВ
```dataview
TABLE WITHOUT ID
  length(filter(allTasks, (t) => !t.completed)) as "Активных задач",
  length(filter(allTasks, (t) => t.completed)) as "Завершено",
  round(length(filter(allTasks, (t) => t.completed)) / length(allTasks) * 100, 1) as "Прогресс %"
FROM #task
```

---

## 🔄 РАБОЧИЙ ПРОЦЕСС

### Ежедневно:
1. Откройте дневник (`Ctrl+D`)
2. Проверьте активные проекты
3. Выберите задачи на день
4. Обработайте Inbox

### Еженедельно:
1. Обработайте все входящие
2. Переместите в PARA
3. Проверьте прогресс проектов
4. Обновите области

---

## 📖 ССЫЛКИ

- [[Инструкция PARA]] — Полное руководство
- [[Дашборд задач]] — Детальный обзор задач
- [[6. Дневник]] — Ежедневные записи

---
**Метод:** PARA | **Версия:** 1.0 | **Обновлено:** `= date(today)`